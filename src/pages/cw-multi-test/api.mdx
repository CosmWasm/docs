---
tags: ["multitest", "API"]
---

# API

## Initialization with the default API

(some description)

- `App::default(){:rust}`
- `app.api(){:rust}`

```rust
let app = App::default();
app.api();
```
