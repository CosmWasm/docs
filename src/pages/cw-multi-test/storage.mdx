---
tags: ["multitest", "storage"]
---

# Storage

## Initialization with the default storage

(some description)

- `App::default(){:rust}`
- `app.storage(){:rust}`
- `app.storage_mut(){:rust}`

```rust
let app = App::default();
app.storage();
app.storage_mut();
```
