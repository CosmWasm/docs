---
tags: []
---

import Tags from "@/components/Tags";

<Tags />

# Store contract code

```rust
// prepare the application with default settings
let mut app = App::default();
// store contract's code
let code_id = app.store_code(counter::contract());
```
